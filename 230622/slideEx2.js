$(".global_nav li h3")
  .eq(0)
  .hover(
    () => {
      $("nav .global_nav li p").eq(0).stop().slideDown();
    },
    () => {
      $("nav .global_nav li p").eq(0).stop().slideUp();
    }
  );
$(".global_nav li h3")
  .eq(1)
  .hover(
    () => {
      $("nav .global_nav li p").eq(1).stop().slideDown();
    },
    () => {
      $("nav .global_nav li p").eq(1).stop().slideUp();
    }
  );

$(".global_nav li h3")
  .eq(2)
  .hover(
    () => {
      $("nav .global_nav li p").eq(2).stop().slideDown();
    },
    () => {
      $("nav .global_nav li p").eq(2).stop().slideUp();
    }
  );

$(".global_nav li h3")
  .eq(3)
  .hover(
    () => {
      $("nav .global_nav li p").eq(3).stop().slideDown();
    },
    () => {
      $("nav .global_nav li p").eq(3).stop().slideUp();
    }
  );
